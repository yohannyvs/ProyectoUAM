@model IEnumerable<PIV_PF_ProyectoFinal.METODOS_PAGO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.metodoPago)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.metodoPago)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idMetodoPago }) |
            @Html.ActionLink("Details", "Details", new { id=item.idMetodoPago }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idMetodoPago })
        </td>
    </tr>
}

</table>
